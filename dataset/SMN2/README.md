# SMN2 Dataset

SMN2 dataset preparation has not been implemented yet.

Recommended future layout:

- `raw/` - source exports before filtering or cleaning.
- `training/` - cleaned training data and feature-engineered outputs.
- `validation/` - independent validation data.
- `notebooks/` - exploratory notebooks, if needed.

When SMN2 data is added, mirror the SOD1 pipeline where possible so model features remain comparable across targets.
